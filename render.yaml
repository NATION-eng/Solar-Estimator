services:
  - type: web
    name: solar-estimator-api
    runtime: node
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5050
    healthCheckPath: /health
